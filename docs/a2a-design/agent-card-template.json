{
  "name": "{{エージェントの識別名（ケバブケース）}}",
  "description": "{{エージェントの責務の説明。1〜3文で記述すること}}",
  "url": "{{エンドポイントURL。例: https://api.example.com/agents/agent-name}}",
  "version": "{{セマンティックバージョン。例: 1.0.0}}",
  "capabilities": {
    "streaming": false,
    "pushNotifications": false
  },
  "authentication": {
    "schemes": ["{{認証方式。例: bearer / apiKey / none}}"]
  },
  "defaultInputModes": ["text"],
  "defaultOutputModes": ["text"],
  "provider": {
    "organization": "{{組織名またはプロジェクト名}}"
  },
  "skills": [
    {
      "id": "{{スキルの識別子。ケバブケースで記述。例: generate-code}}",
      "name": "{{スキルの表示名}}",
      "description": "{{このスキルで実行できる具体的なタスクの説明}}",
      "inputModes": ["text"],
      "outputModes": ["text"]
    }
  ]
}
